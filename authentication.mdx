---
title: Authentication
icon: key
iconType: light
description: Access search and browser services.
---

import Terms from '/snippets/search/terms.mdx';
import Url from '/snippets/browser/url.mdx';

## Bearer tokens

You can authenticate with every **Agent First** API endpoint by including
[your secret token](https://dashboard.agentfirst.dev/profile) in an `Authorization` header:

```http
Authorization: Bearer [Insert token here]
```

Here are example requests that use the [common Curl command](https://curl.se/):

<Terms />

<Url />
